import { PersonalPageDynamicHostDirective } from './personal-page-dynamic-host.directive';

describe('PersonalPageDynamicHostDirective', () => {
  it('should create an instance', () => {
    const directive = new PersonalPageDynamicHostDirective();
    expect(directive).toBeTruthy();
  });
});
